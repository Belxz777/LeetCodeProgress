# Не решал задачи 164 дня 
## Решение с двумя указателями пояснение я в коде оставил 
## Если в кратце:
1.Мы просто итерируем 
# Код
```typescript []
function threeSum(nums: number[]): number[][] {
    // nums = [-1,0,1,2,-1,-4]
    // let sorted = [];
    // let defset = new Set()
    // for(let i =0;i<nums.length;++i){
    //     for(let j = i+1;j<nums.length;++j){
    //         for(let k = j+1;k<nums.length;++k){
    //             if(nums[i] + nums[j] + nums[k] == 0 ){
    //                 const triplet = [nums[i],nums[j],nums[k]].sort((a,b)=>a-b)
    //                 const key = triplet.join(',')
    //                 // мы делаем это что бы обойти ограничение по ссылкам на array
    //                 // потому что js проверяет по индексу , а не по значению
    //                 if(!defset.has(key)){
    //                     defset.add(key);
    //                     sorted.push(triplet)
    //                 }
    //             }
    //         }
    //     }
    // }
    // return sorted;
    // данное решение работать не будет (здесь) так как 311 кейсе мы упираемся по время исполнения
    // решение с двумя указателями 

    nums.sort((a,b)=>a-b);
    let res = [];
    for (let i=0;i<nums.length -2;++i){
        if(nums[i]>0){
            break;
        } // не понял ланную проверку
        if (i>0 && nums[i]===nums[i-1]){
            continue;
        } // проврека на дубликаты i 
        let left = i + 1
        let right = nums.length - 1 
        // создаем два указателя 
        while (left < right) {
        // итерируем пока указатели не сошлись
        let sum = nums[left] + nums[i] + nums[right];
        // на каждой итерации считаем сумму
        if(sum == 0){
            // если 0 то все ок пушим в результат
            res.push([nums[i],nums[left],nums[right]])
            // проверяем на дубликаты впереди левого указатели если что перепрыгиваем
            while(left < right && nums[left]===nums[left+1]){ left++}
            // проверяем дубликаты правого тоже самое только проверяем не след , а предыдущий
            while(right>left && nums[right]===nums[right-1]){ right--}
            left++
            right--
        }
        else if (sum < 0){
            // если сумма меньше нуля то мы увеличиваем left тем самым мы увеличиваем сумму так как числа в массиве отсортированны по порядку
            left++
        }
        else if (sum > 0){
            // обратное делаем с right
            right--
        }
        }
    }
    return res

};
```
